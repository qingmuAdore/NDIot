var UUID = require('uuid');
var HELP = require('../../app/utils').HELP;

console.log(UUID.v1());


console.log(HELP.uuid());

console.log(HELP.uuid());

console.log(HELP.uuid());

console.log(HELP.uuid());

console.log(HELP.uuid());

console.log(HELP.uuid());
